# F-16: perfil sincronizado, invariante de plazos y CI

> **Estado:** diseño aprobado. Pendiente de implementación.
> **Fecha:** 14 de septiembre de 2026.
> **Origen:** hallazgo F-16 de `2026-09-13-remediacion-auditoria-seguridad.md` (sección 4), más el
> test con carrera de la sección 5 ("Extra").
> **Rama:** `fix/f16-perfil-sincronizado-y-ci`.

## 1. Objetivo

Que ningún arreglo posterior en los scripts de capa 2 pueda quedar fuera del perfil que realmente se
ejecuta, que la invariante de plazos deje de sostenerse a mano y que exista una CI que corra la
suite adversaria contra Docker real.

Es el primer sub-proyecto de la remediación. Los siguientes (verificador de bytecode, `capa1.sh`,
worker y ejecutor, cadena de suministro, operación) dependen de esta base.

## 2. Estado actual

- `ejecutor/Dockerfile` copia `perfiles/` entero a la imagen. `Catalogo` lee sólo los `*.json`: el
  `.sh` viaja pero no se usa.
- El campo `script` de `perfiles/java21-junit@4.json` es una copia a mano de
  `perfiles/java21-junit.sh`. Lo mismo pasa con `pruebas/perfiles-ejemplo/java21-junit-tiempos@1.json`.
  Al 14 de septiembre de 2026 los dos pares coinciden byte a byte; nada lo garantiza.
- La invariante de plazos está repartida en cuatro lugares:
  - `TIMEOUT_COMPILE_S=8` y `TIMEOUT_TESTS_S=25` en `perfiles/java21-junit.sh`.
  - `SANDBOX_EVAL_TIMEOUT_S=45` en el `ENV` de `imagenes/java21-junit/Dockerfile`.
  - El valor por defecto `${SANDBOX_EVAL_TIMEOUT_S:-45}` y la gracia `timeout -k 5s` en
    `imagenes/capa1/capa1.sh`.
  - `TIMEOUT_EJECUCION_MS = 60_000` en `ejecutor/.../Constantes.java`.
- No hay CI en el repositorio.
- `BundlesIT.p4c_hostilReporteLoop` espera `exitEval == 0`, pero a veces obtiene 47.

## 3. Decisión: el `.sh` es la fuente

Se evaluaron tres opciones:

| Opción | Resumen | Por qué no / por qué sí |
|---|---|---|
| **A — `.sh` fuente + regeneración + test de sincronía** | Se edita sólo el `.sh`; un test falla si el JSON difiere y sabe regenerarlo | **Elegida.** No cambia el formato del perfil ni `Catalogo` |
| B — Generar el JSON en el build | El JSON deja de versionarse | `BundlesIT` lee `../perfiles` directo y el perfil deja de ser legible en el repo |
| C — Referenciar el script por ruta y hash | `scriptRuta` + `scriptSha256` en el JSON | Cambia el formato del perfil y `Catalogo`; en el modelo real el script llega de G5 dentro del perfil |

## 4. Diseño

### 4.1 `PerfilesSincroniaTest`

Ubicación: `ms-sandbox/ejecutor/src/test/java/sandbox/ejecutor/PerfilesSincroniaTest.java`.

**Qué recorre.** Los directorios `../perfiles` y `../pruebas/perfiles-ejemplo`, relativos al módulo
`ejecutor` (la misma convención que usa `BundlesIT` con `Path.of("..", "perfiles")`).

**Qué exige, para cada archivo `<id>@<version>.json`:**
1. Existe `<id>.sh` en el mismo directorio.
2. El valor del campo `script` es igual al contenido de `<id>.sh`, leído como bytes UTF-8.

Si algo falla, el mensaje nombra el par y el comando de regeneración.

**Regeneración.** Con la propiedad de sistema `perfiles.regenerar=true`, el test no falla: reescribe
el campo `script` de cada JSON desincronizado.

```
mvn test -Dtest=PerfilesSincroniaTest -Dperfiles.regenerar=true
```

- Reemplaza sólo el literal de `script` dentro del texto del JSON. El valor nuevo se serializa con
  Jackson (`ObjectMapper.writeValueAsString`). El resto del archivo (orden de campos, indentación,
  saltos de línea) no se toca.
- Si después del reemplazo el JSON no parsea o el `script` leído no coincide con el `.sh`, el test
  falla sin escribir el archivo.
- El JSON se escribe en UTF-8.
- Surefire reenvía las propiedades de sistema de la línea de comandos a la JVM de los tests, así que
  no hace falta configuración extra en el `pom.xml`. Se verifica al implementar.

**Regla asumida.** Un `.sh` por `perfilId`. Si en el futuro conviven dos versiones del mismo perfil
con scripts distintos, esta regla se revisa.

### 4.2 `PlazosInvarianteTest`

Ubicación: `ms-sandbox/ejecutor/src/test/java/sandbox/ejecutor/PlazosInvarianteTest.java`.

**Qué lee (siempre de los archivos reales):**

| Valor | Fuente | Extracción |
|---|---|---|
| `TIMEOUT_COMPILE_S`, `TIMEOUT_TESTS_S` | cada `*.sh` de `../perfiles` | línea `^NOMBRE=(\d+)` |
| `SANDBOX_EVAL_TIMEOUT_S` | `../imagenes/java21-junit/Dockerfile` | `SANDBOX_EVAL_TIMEOUT_S=(\d+)` |
| Valor por defecto del backstop | `../imagenes/capa1/capa1.sh` | `\$\{SANDBOX_EVAL_TIMEOUT_S:-(\d+)\}` |
| Gracia del backstop | `../imagenes/capa1/capa1.sh` | `timeout -k (\d+)s` |
| Reloj del ejecutor | `Constantes.TIMEOUT_EJECUCION_MS` | acceso directo (mismo paquete) |

**Qué verifica:**
1. Para cada perfil de producción: `2 × TIMEOUT_COMPILE_S + TIMEOUT_TESTS_S + 2 ≤ SANDBOX_EVAL_TIMEOUT_S`.
   El margen mínimo es de **2 s** (constante con nombre en el test).
2. `SANDBOX_EVAL_TIMEOUT_S + gracia < TIMEOUT_EJECUCION_MS / 1000`.
3. El valor por defecto de `capa1.sh` es igual al `ENV` del Dockerfile.
4. Si una variable no aparece en su fuente, el test falla. No se saltea.

**Cambio de documentación asociado.** El comentario *"INVARIANTE (nadie la valida, se sostiene a
mano)"* de `perfiles/java21-junit.sh` pasa a decir que la valida `PlazosInvarianteTest`. Como el
`.sh` cambia, el JSON se regenera con 4.1.

Los perfiles de `pruebas/perfiles-ejemplo` no entran en esta verificación: no son de producción.

### 4.3 CI

Archivo: `.github/workflows/ms-sandbox.yml`, en la raíz del repositorio.

**Disparadores.** `push` y `pull_request` con `paths: ["ms-sandbox/**", ".github/workflows/ms-sandbox.yml"]`.

**Entorno.** `ubuntu-latest`, Temurin 21 con caché de Maven.

**Jobs:**

| Job | Pasos | Qué cubre |
|---|---|---|
| `worker` | `mvn -B verify` en `ms-sandbox/worker` | Tests unitarios. `D16IT` se saltea sola (no hay ejecutor levantado) |
| `ejecutor` | 1. `docker build -f imagenes/java21-junit/Dockerfile -t sandbox-runner:2.0.0-capa1 imagenes` (desde `ms-sandbox/`). 2. Construir `sandbox-runner:1.0.0` desde `ejecutor/src/test/fixtures/a6`. 3. `mvn -B verify` en `ms-sandbox/ejecutor` | Unitarios, `PerfilesSincroniaTest`, `PlazosInvarianteTest`, y `BundlesIT` + `ProtocoloIT` contra el Docker del runner |

El comando exacto de build del fixture `a6` se toma de su `Dockerfile` o de la documentación del
test al implementar.

**Fuera de alcance.** SBOM y escaneo de dependencias e imágenes: van al sub-proyecto de cadena de
suministro (F-07, F-08).

### 4.4 Test con carrera

`BundlesIT.p4c_hostilReporteLoop` pasa a aceptar `exitEval ∈ {0, 47}`. Las aserciones que detectan
el ataque (`VEREDICTO_NO_CONFIABLE`, `surv == 6`, `exitCode == 30`) no cambian. Un comentario
explica por qué 47 también es válido: la guarda de `tests=0` puede ganarle a la carga hostil.

## 5. Criterios de aceptación

- Modificar `perfiles/java21-junit.sh` sin regenerar el JSON hace fallar `mvn verify` en `ejecutor`.
- Correr la regeneración deja el JSON sincronizado y cambia sólo el campo `script`.
- Subir `TIMEOUT_TESTS_S` a 30 (8 + 8 + 30 + 2 > 45) hace fallar `PlazosInvarianteTest`.
- El workflow corre en verde en la rama, con `BundlesIT` ejecutada (no salteada).

## 6. Entrega

Commits en `fix/f16-perfil-sincronizado-y-ci`, con conventional commits:
1. Este documento.
2. `PerfilesSincroniaTest`.
3. `PlazosInvarianteTest` y el comentario del `.sh` con su JSON regenerado.
4. El test con carrera.
5. El workflow de CI.

El PR a `main` se abre después de validar la rama.
